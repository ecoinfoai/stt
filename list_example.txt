# Example media list for batch_stt.py / run_list.bat / run_list.sh
# One title per line, relative to the data/ folder. The extension
# is optional - a bare title finds the matching media file, and an
# audio file wins over a video file of the same name.
# Lines starting with '#' are comments.
#
#     uv run batch_stt.py --list list.txt --dry-run
#
# Use --dry-run first: every title is resolved to a real file
# before any transcription starts, so typos surface immediately.

[기업가정신 1편] 발명가와 기업가의 차이, 기업가정신
[기업가정신 2편] 기업가정신이란 무엇일까
sub/한 단계 아래 폴더에 있는 강연
정확한 파일명을 적어도 됩니다.m4a
