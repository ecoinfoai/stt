# Dependencies only (uv users: see pyproject.toml / uv sync).
# Install: python -m pip install -r requirements.txt
# The `stt` command itself needs: python -m pip install -e .
faster-whisper==1.2.1
yt-dlp>=2025.1
pyyaml>=6
