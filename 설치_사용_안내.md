# 강연 전사 도구 — 설치·사용 안내 (Windows 11)

영상(.mp4 .mkv …)이나 녹음(.m4a .mp3 .wav …)에서 한국어 전사문(.txt)을
만드는 도구입니다. faster-whisper(Whisper 계열 음성인식)를 노트북의
RTX 3060 GPU에서 돌리며, VRAM이 부족하면 더 작은 모델로 자동
폴백합니다. 인터넷에 음성을 올리지 않고 전부 로컬에서 처리합니다.

폴더 구성:

```
transcribe.py        전사 스크립트 (본체)
전사.bat             끌어다 놓기 실행용 (선택)
requirements.txt     파이썬 의존성
terms_example.txt    용어 파일 예시
tests/               단위·통합 테스트 (개발 검증용, 선택)
설치_사용_안내.md    이 문서
```

---

## 1. 최초 1회 설치

### 1-1. 파이썬 확인

PowerShell을 열고:

```powershell
py --version
```

3.11 이상이면 그대로 진행하세요. 없거나 3.10 이하라면
https://www.python.org/downloads/ 에서 3.12를 설치하되, 설치 첫
화면에서 **"Add python.exe to PATH"를 반드시 체크**하세요.

### 1-2. 가상환경 만들기 + 패키지 설치

가상환경은 OneDrive 밖(사용자 홈)에 만듭니다. OneDrive 안에 만들면
수천 개 파일이 동기화되어 느려집니다.

```powershell
py -m venv "$env:USERPROFILE\.venvs\stt"
& "$env:USERPROFILE\.venvs\stt\Scripts\python.exe" -m pip install -U pip
& "$env:USERPROFILE\.venvs\stt\Scripts\python.exe" -m pip install -r "이 폴더 경로\requirements.txt"
```

(`이 폴더 경로`는 이 안내서가 있는 폴더, 예:
`C:\Users\pow51\localgit\stt`)

### 1-3. GPU 라이브러리 (cuBLAS + cuDNN 9)

GPU 실행에는 NVIDIA 라이브러리 두 개가 필요합니다
(CUDA 12용 cuBLAS, CUDA 12용 cuDNN 9 — faster-whisper 공식 요구사항).
가장 간단한 방법:

1. https://github.com/Purfview/whisper-standalone-win/releases 접속
2. **"libs"** 릴리스에서 **`cuBLAS.and.cuDNN_CUDA12`** 로 시작하는
   zip을 내려받기
3. 압축을 풀어 나온 DLL 파일들을 전부
   `%USERPROFILE%\.venvs\stt\Scripts` 폴더에 복사

(python.exe와 같은 폴더에 두면 별도 PATH 설정 없이 인식됩니다.
대안: NVIDIA 공식 사이트에서 CUDA 12용 cuDNN 9 설치.)

### 1-4. 설치 확인

```powershell
nvidia-smi
& "$env:USERPROFILE\.venvs\stt\Scripts\python.exe" -c "import ctranslate2; print('GPU 인식:', ctranslate2.get_cuda_device_count())"
```

`nvidia-smi`에서 VRAM 총량을 확인해 두세요(6GB인지 12GB인지).
`GPU 인식: 1`이 나오면 준비 완료입니다. `0`이면 NVIDIA 드라이버를
업데이트한 뒤 다시 확인하세요.

---

## 2. 사용법

### 기본 (PowerShell)

```powershell
& "$env:USERPROFILE\.venvs\stt\Scripts\python.exe" .\transcribe.py "강연.mp4"
```

또는 탐색기에서 **영상/녹음 파일을 `전사.bat` 위로 끌어다 놓기**
— 같은 동작을 합니다. 결과는 원본 옆에 같은 이름의 `.txt`로
저장됩니다.

### 자주 쓰는 형태

```powershell
# 녹음 파일(STT)도 동일
python .\transcribe.py "회의녹음.m4a"

# 폴더째 일괄 처리 (이미 전사된 파일은 자동으로 건너뜀)
python .\transcribe.py "D:\강의영상"

# 용어 표기 유도 + SRT 자막도 함께
python .\transcribe.py "강연.mp4" --terms terms_example.txt --srt

# 타임스탬프 없는 순수 텍스트
python .\transcribe.py "강연.mp4" --no-timestamps
```

(위 예시의 `python`은 venv의 python.exe 경로를 뜻합니다.
`전사.bat`를 쓰면 경로를 신경 쓸 필요가 없습니다.)

### 명령 짧게 만들기 (alias)

PowerShell 프로필에 함수 한 줄을 넣으면 어느 폴더에서든
`stt 파일명`으로 실행할 수 있습니다. 한 번만 설정하면 됩니다:

```powershell
if (!(Test-Path $PROFILE)) { New-Item -ItemType File -Path $PROFILE -Force | Out-Null }
Add-Content $PROFILE 'function stt { & "$env:USERPROFILE\.venvs\stt\Scripts\python.exe" "C:\Users\pow51\localgit\stt\transcribe.py" @args }'
. $PROFILE
```

이후에는:

```powershell
stt "강연.mp4"
stt "녹음.m4a" --terms terms_example.txt --srt
```

### 주요 옵션

| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--model` | auto | auto는 large-v3 → large-v3-turbo → small 순 폴백 |
| `--device` | auto | GPU 있으면 cuda, 없으면 cpu |
| `--language` | ko | `auto`면 언어 자동 감지 |
| `--terms 파일` | 없음 | 용어 파일로 전문용어 표기 유도 |
| `--no-timestamps` | 끔 | 문단 앞 `[MM:SS]` 제거 |
| `--srt` | 끔 | SRT 자막 파일도 저장 |
| `--overwrite` | 끔 | 기존 결과를 다시 만듦 |
| `--output-dir 폴더` | 원본 옆 | 결과 저장 위치 지정 |
| `--beam` | 5 | 1로 낮추면 빨라지고 정확도 약간 하락 |
| `--gap` | 2.0 | 문단을 나누는 무음 길이(초) |
| `--max-chars` | 800 | 문단 최대 글자 수 |

### 출력 예

```
# 원본: 강연.m4a
# 모델: faster-whisper large-v3 | 언어: ko | 길이: 15:03

[00:00] 안녕하십니까. 오늘은 4차 산업혁명이 무엇인지 이야기해
보겠습니다. …

[01:42] 다음으로 기업가정신 이야기를 해 보죠. …
```

---

## 3. 알아둘 점

**첫 실행 시 모델 다운로드.** 처음 한 번은 Hugging Face에서 모델을
받습니다 — large-v3 약 3.1GB, large-v3-turbo 약 1.6GB, small 약
0.5GB. `%USERPROFILE%\.cache\huggingface`에 저장되어 이후에는 바로
실행됩니다.

**속도 감각.** RTX 3060 기준 15분 강연이 대략 2~4분(large-v3),
그보다 빠르게는 turbo. CPU로만 돌리면 실시간보다 느릴 수 있습니다.

**VRAM 6GB일 때.** large-v3(int8)가 아슬하게 들어갑니다. 만약
메모리 부족이 나면 스크립트가 자동으로 turbo → small로
내려가므로 그대로 두면 됩니다. 처음부터 빠르게 가려면
`--model large-v3-turbo`를 지정하세요 — 품질 차이가 크지 않습니다.

**품질 팁.** 강연마다 용어 파일을 만들어 `--terms`로 넘기면
전문용어·인명 오기가 눈에 띄게 줄어듭니다. `terms_example.txt`를
복사해서 고쳐 쓰세요.

---

## 4. 문제 해결

| 증상 | 조치 |
|---|---|
| `cudnn` / `cublas` DLL 오류 | 1-3의 DLL들이 venv `Scripts` 폴더에 있는지 확인 |
| `GPU 인식: 0` | NVIDIA 드라이버 업데이트 후 `nvidia-smi` 재확인 |
| 메모리 부족 반복 | `--model small` 또는 `--device cpu`로 실행 |
| 너무 느림 | `--beam 1` 또는 `--model large-v3-turbo` |
| 결과가 비었다는 오류 | 무음 파일인지 확인, `--language auto`로 재시도 |
| 모델 다운로드 실패 | 인터넷 연결 확인 후 재실행(이어받기 됨) |

개발 검증용 테스트를 직접 돌려보려면:

```powershell
& "$env:USERPROFILE\.venvs\stt\Scripts\python.exe" -m pip install pytest
& "$env:USERPROFILE\.venvs\stt\Scripts\python.exe" -m pytest tests\
```
